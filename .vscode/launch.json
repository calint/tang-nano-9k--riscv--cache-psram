{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "osqa: debug console application",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/os/console_application",
            "args": [
                "notes/samples/sample.txt"
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "osqa: build console application",
        }
    ]
}