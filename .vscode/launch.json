{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "os: debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/os/console_application",
            "args": [],
            "cwd": "${workspaceFolder}",
            //"preLaunchTask": "glos: build",
        }
    ]
}